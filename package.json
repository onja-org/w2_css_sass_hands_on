{
  "name": "candy-expo-sass-workshop",
  "version": "1.0.0",
  "description": "A hands-on workshop for learning SASS through building a candy expo website",
  "main": "index.js",
  "scripts": {
    "build": "sass --watch scss:css --style=expanded",
    "build-once": "sass scss:css --style=expanded",
    "dev": "sass --watch scss:css --style=expanded",
    "clean": "rm -rf css/"
  },
  "keywords": ["sass", "css", "workshop", "learning"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.69.0"
  }
}